import React from 'react';

const Profile = ({ character }) => {
    
    return (
        <div class="riche-img">
            <div>
            <style>
            background-image: url(character.pics.profile);
            </style>
            </div>
        </div> 
    )
};

export default Profile;